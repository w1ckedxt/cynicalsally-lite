# Session 2026-03-25 (c)

## Wat is gedaan

### cynicalsally-lite
- server.js monolith gesplit in 3 modules: server.js, lib/github.js, lib/html.js
- Deploy to Render button op pagina (boven + onder) met SALLYXRENDER.png + Shifra's UTM
- Window title: "Sally Lite | Same sharp feedback, simpler to run"
- README deploy button met utm_source=sally_website
- Target (repo URL/filename) meesturen naar backend voor admin tracking

### cynicalsally-render (admin)
- Admin visual upgrade: translucent badges, glow effects, Sally's Back Office branding, tab bar glow, live dot, tabular-nums, semantic KPI kleuren, Inter font
- Lite tracking: LITE_REVIEW event code, source "lite" in events
- Source tracking uitgebreid: chrome_ext, safari_ext, lite types
- Target tracking: classify, roast, truth routes accepteren client target
- is_test systeem verwijderd — alle events tellen nu mee
- Safari ITP workaround: deviceId fallback auth in /api/v1/auth/session
- Content script in Safari extension zet deviceId in localStorage
- Website useDeviceId() prefereert extension deviceId boven web-generated

### cynicalsally-render (roast engine)
- Dynamic scoring categories per content type (was hardcoded website categories)
- content_type doorpassen van classify → QR/FT prompts
- QR prompt krijgt content type hint (geen re-triage nodig)
- FT prompt krijgt dynamische scoring categories via buildFtSystemPrompt()
- Ethische regels uitgebreid: minderjarigen, PII, career gaps, discriminatie taal, voedselveiligheid, non-native writing
- 44+ content types met elk 7 domain-expert scoring categories
- GPT-5.2 geconsulteerd voor categories en type-specifieke ethical concerns

### cynicalsally-safari
- Content script (content.js) toegevoegd voor deviceId bridge
- manifest.json updated met content_scripts voor cynicalsally.com

## Nog open
- Safari auth: werkt na hard refresh, timing/race condition bij eerste bezoek
- Chrome extension: target (page URL) en source ("chrome_ext") toevoegen aan fetch body
- Backend API routes zonder deviceId fallback: /api/v1/account, /api/v1/chat/history, /api/v1/chat/messages
- Admin visual upgrade: rest van het plan (OpsTab color-coded pills, etc.)
- Visitor stats/home page in admin (Thomas wil dit later)

## Volgende stappen
- Chrome extension update (target + source) — simpele change, geen permission changes
- Safari auth race condition oplossen (retry of delay)
- Admin visitor stats uitbreiden
- Testen van alle content type scoring met echte content
